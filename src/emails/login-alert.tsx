/**
 * @deprecated Prefer NewDeviceLoginEmail — login alerts are merged into the new-device template.
 * Kept so existing imports/previews resolve during transition.
 */
export { default } from "./new-device-login";
